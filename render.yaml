services:
  - type: web
    name: backend
    env: docker
    plan: free
    branch: main
    rootDir: backend
    dockerfilePath: backend/Dockerfile
    envVars:
      - key: PORT
        value: 10000
